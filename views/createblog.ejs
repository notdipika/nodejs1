<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #000000;
            margin: 0;
            padding: 0;
        }
        nav {
            background-color: #333;
            color: #fff;
            padding: 10px 20px;
        }
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
        nav ul li {
            float: left;
        }
        nav ul li a {
            display: block;
            color: white;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
        }

        #blog-creator {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        #blog-form label {
            display: block;
            margin-bottom: 5px;
        }

        #blog-form input[type="text"],
        #blog-form input[type="url"],
        #blog-form textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        #blog-form button {
            background-color: #333;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 4px;
            font-size: 16px;
        }

        #blog-form button:hover {
            background-color: #555;
        }

        .blog-post {
            margin-top: 20px;
            padding: 10px;
            background-color: #f9f9f9;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        .blog-post h2 {
            margin-bottom: 5px;
        }

        .blog-post img {
            max-width: 100%;
            height: auto;
            border-radius: 4px;
        }

        .blog-post p {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <%- include("navbar.ejs") %>

    <section id="blog-creator">
        <h1>Create Your Blog</h1>
        <form id="blog-form" action="/createblog" method="post" enctype="multipart/form-data">
            <label for="blog-title">Title:</label>
            <input type="text" id="blog-title" name="title" required>

            <label for="blog-subtitle">Subtitle:</label>
            <input type="text" id="blog-subtitle" name="subtitle">

            <label for="image">Image:</label>
            <input type="file" id="image" name="image" accept="image/*" required>
            
            <label for="blog-description">Description:</label>
            <textarea id="blog-description" name="description" rows="4" required></textarea>

            <button type="submit" onclick="return createBlogPost()">Create Blog</button>
        </form>
    </section>

</body>
</html>